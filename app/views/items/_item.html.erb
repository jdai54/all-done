
<tbody id='item-<%= item.id %>'>
  <tr>
    <td class="check"><%= link_to "", user_item_path(@user.id, item.id), method: :delete, class: 'glyphicon glyphicon-ok', remote: true %></td>
    <td><%= item.name %></td>
    <td><%= distance_of_time_in_words(item.created_at + 7.days, Time.now) %> remaining</td>
  </tr>
</tbody>
